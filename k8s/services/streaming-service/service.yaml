apiVersion: v1
kind: Service
metadata:
  name: streaming-service
  namespace: hustsim
  labels:
    app: streaming-service
    app.kubernetes.io/part-of: hustsimulator
spec:
  type: ClusterIP
  selector:
    app: streaming-service
  ports:
    - port: 8084
      targetPort: 8084
      name: http
